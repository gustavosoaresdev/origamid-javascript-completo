const comida = 'Pizza';
const liquido = new String('Água');
const ano = new String(2022);

console.log(liquido.toUpperCase());

/* ================================================================================= */